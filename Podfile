#
# See https://github.com/CocoaPods/Specs
#

platform :ios, '7.0'

target :OctoKitDemoTests, :exclusive => true do
  pod 'Specta',  '~> 0.2'
  pod 'Expecta', '~> 0.2' # expecta matchers
  pod 'OCMock',  '~> 2.2' # OCMock
end
